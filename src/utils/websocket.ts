import { Token } from '@/utils/utils'
import { notification, message } from 'ant-design-vue'

// const wsUrl = `${document.location.protocol.replace('http', 'ws')}//${document.location.host}/jetlinks/messaging/${Token.get()}?:X_Access_Token=${Token.get()}
